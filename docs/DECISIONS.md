# Decisions taken in the rebuild

Archive decisions are cited as D-numbers and live in the archive's `docs/DECISIONS.md`.
Each entry here records evidence, assumptions, rejected alternatives and the trigger that
would reopen it.

| # | Decision | Evidence and assumptions | Rejected | Reopen when |
|---|---|---|---|---|
| RB1 | One declared workspace root (`TENNISLAB_WORKSPACE`, default the current directory); every path resolves under it and input containment is checked lexically, without following links; write paths additionally use physical symlink refusal under RB16. | The archive computed its root from each source file's location and 22 tracked files carried absolute paths (assessment §1). Lexical containment lets an equivalence workspace link read-only inputs from the archive while every write lands inside it. | Multiple read roots with an allowlist (more configuration, same guarantee); `Path.resolve()` containment (breaks the linked workspace). | A deployment needs inputs from more than one root. |
| RB2 | Code provenance is a package receipt (module name, file hash, package version), not a repository path. Config fields that pinned a module by path and hash are read and recorded as `declared_binding`; the package module runs. | 41 archive files imported modules by path at a pinned hash (assessment §1). A package cannot hash "the file that ran" by repository path. Data bindings keep their hash checks. | Verifying the archive file's hash against the package file (meaningless after a port). | A run needs to prove it executed a specific historical code version: use the git tag plus the receipt. |
| RB3 | Label barrier contract. Fold fit/selection reads use declared history accessors; state replay and panel-building stages declare historical reads and retain their native chronological cursors. Proper scores are emitted only by post-barrier evaluation stages (RB14/RB16). The test that runs the chain with the final target year's labels absent must pass for every stage before the report. | SCAR_TISSUE B1 asks for "the label file physically absent" before the report. In a walk-forward chain every target year is a training year of a later fold and the panel itself carries the outcome column, so literal absence is not attainable for the fit stage. The prospective situation is the final target year's outcomes being unknown; that is what the test encodes. | Physical absence for all stages (impossible for fits); per-fold processes (no equivalence with the accepted runs). | Lane C's leakage suite finds a pre-report score of a target year. |
| RB4 | Product commits carry the GitHub noreply address of the account `gh` is logged into. | R8 requires a GitHub-attributable email; the owner's GitHub email was not available in the session (`gh` token invalid) and the noreply address is attributable by construction. | The `.local` address (not attributable); guessing the account email. | The owner supplies the email: rewrite the short history with `git filter-repo --mailmap` before the first push. |
| RB5 | Provenance-only differences from the archive outputs are recorded as "explained", never patched to identical. | A launcher id, a code hash or a code path in a manifest is exactly the thing a port changes; forcing equality would mean writing the archive's values into the new code. Data outputs must be identical. | Emulating archive provenance strings. | Never. |
| RB6 | Data artifacts carry no wall-clock stamp; the stage manifest records start and finish times. | `edition_index.json` embedded `generated_at_utc`, so an otherwise identical rebuild could never be byte-identical and every downstream PENDING hash changed on each run. | Keeping the stamp behind a flag (two output shapes). | Never. |
| RB7 | Outputs that embed the hash of a rewritten source workbook are expected to differ from the archive on every row of that season, and are recorded as a cascade of one provenance difference, not as data differences. | The bridge rewrites the 2026 WTA tennis-data workbook; the archive's bytes carried openpyxl's wall-clock `docProps` timestamps, the rebuild fixes ZIP entry times and the created property, but B2's independent rerun found openpyxl resets the modified property during save. Strict workbook byte determinism is therefore not established (correction to the earlier RB6 implementation claim). `market_rows.csv` and both `panel.csv` files carry a `market_source_sha256` column, so 2,095 join rows and 1,945 panel rows differ in that one column only; every other cell reproduces. | Reproducing the archive's timestamps to force hash equality (would make the artifact wall-clock dependent again). | A separately tested workbook serialization repair removes the remaining modified-time variation, or the column stops being a hash of workbook bytes. |
| RB8 | Preserve existing authorship and remote history. For this handoff's new local commits use the explicitly supplied noreply email; do not rewrite or push. | Astra verified 31 baseline commits: 23 Gmail, 8 noreply; the configured remote already advertises an earlier commit. The handoff's all-noreply/no-remote/unpushed description is stale. This does not reopen the owner's R8 account decision or verify GitHub attribution. | Rewriting on the assumption nothing was pushed; guessing account state; changing global Git identity. | The owner explicitly requests an attribution repair or remote integration after reviewing the actual histories. |
| RB9 | Tier stages must join the synthetic acceptance path before a prospective full_tier campaign; preserve the existing sample as the baseline. | The accepted sample bypasses all tier stages, while full_tier is the strongest sports rung. Individual tier unit tests do not establish whole-chain mutation invariance. Native tier/SR02/sidecar T1 and full-bundle T3 remain requirements of Lane C2 integration. No change to tier_serve_counts_start_year is authorised by this decision. | Treating the non-tier sample as full_tier coverage; changing a frozen tier horizon to simplify a fixture. | A separately registered campaign deliberately excludes full_tier; retained historical equivalence still needs its existing tests. |
| RB10 | Keep frozen historical workspace bindings for reconstruction; Lane D creates a separate versioned update workspace and manifest-bound live config. | The two real chain descriptors consume archive-relative evidence paths. Replacing those paths in place would obscure the equivalence oracle. Reuse the existing single TENNISLAB_WORKSPACE root, immutable source versions and explicit output directories; no second root framework. | Reorganising archive work directories; silently pointing historical configs at the latest snapshot; adding a generic data registry. | A qualified source layout cannot be represented by the existing manifest/path contracts. |
| RB11 | SR03 forecast-only and complete label/read receipts are integration prerequisites under R9, not a Lane E scientific choice. Preserve baseline 180cb2d for historical equivalence; repair in a separately recorded revision and independently reconstruct it. | calibrate.py calls calibration.evaluate before the barrier; test_label_barrier only withholds the final target year; runner.py writes a static outcome_access flag. R9 and the independent C rejection invalidate the stronger barrier claim. Legitimate training and selection consume past outcomes relative to each fold; a global earliest-year ceiling would break the frozen walk-forward estimand. Require fold-specific read receipts and no emitted outer-year metrics before report; document that clarification before implementation. | Deferring the repair to a campaign; claiming removed metrics remain byte-identical; forbidding later folds from consuming legitimately past years; treating a returned key filter as physical non-access to all file bytes. | The repaired chain passes planted runtime leaks, unchanged forecast hashes and independent review; then close the prerequisite, retaining the historical warning. |
| RB12 | No archive integration commit or Lane C CI port until the lane evidence is reconstructable and each imported detector fails on its declared planted defect. | R15 rejects the original suite as comprehensive; no C2 report or negative-control acceptance table was present at handoff review. Lane A's existing reconstruction file is a prompt, not a completed review. Preserve lane-owned additions and the original C report; use separate reconstruction prompts/results. | Committing other lanes' untracked work on a status-only review; copying known false-negative gates into CI; equating a skipped test with a passed test. | A/C2 independent reports and per-test positive/negative evidence exist with clean write boundaries. |
| RB13 | Lane D treats event anchors as event identifiers, never as evidence that a result was available. Require an admissible result-date upper bound and receipt/publication qualification at the declared cutoff; quarantine unresolved overlapping history. | R12/UPDATE_SPEC describe D-2 on anchors but also require withholding unresolved overlaps. chronology.dating rejects EVENT_ANCHOR as availability evidence; the Canada regression shows why. Keep R6 historical equivalence unchanged and record live bounds separately; charter_asserted remains distinct under R13. | Releasing all final-round results at event start; treating an event-end bound as proof of publication; promoting a charter date to official after a spot check. | A permitted source qualifies per-match completion/publication timing, or a pre-registered sensitivity explicitly changes the cutoff. |
| RB14 | Fold-specific outcome horizons and a content-scanned barrier (Lane B2 design, 2026-09-14). Fold-specific outcome reads use an accessor with the consuming fold's ceiling (outer year − 1); declared historical state replays retain their native D−2 cursor, whose dataflow is tested separately; the runner derives each stage's observed outcome access from an audit hook on file opens and refuses undeclared readers or receipts beyond their fold; the barrier refuses to freeze a run tree containing any metric-shaped artifact; SR03 component metrics and the pipeline's selection-criterion scores move behind the barrier and are recomputed there against a pre-barrier commitment hash. | R9's global-ceiling wording is unimplementable for the frozen walk-forward estimand (later folds train on earlier outer years); RB11 resolves it as fold-specific horizons plus zero emitted outer-year scores. C2 T8b flagged 52 ATP / 22 WTA pre-barrier metric artifacts; the archive's `outcome_access` flag was static. Filtering returned keys is not physical non-access, so receipts record rows parsed and rows returned separately. Design: `docs/INTEGRITY.md`. | A global earliest-outer-year ceiling (breaks the estimand); renaming metric keys to evade the scanner; keeping the static flag; per-fold subprocesses. | The repaired chain fails a planted leak it should catch, or a forecast hash differs from `180cb2d`. |
| RB15 | Two synthetic scenarios, both mandatory in CI (2026-09-14, Lane B2). `data/sample` (base, pinned full − base 0.0010578138814451785, n 1,578) is unchanged; `data/sample_tier` runs the five tier stages natively (pinned full_tier − full equal-year −0.0005904833739188241, n 1,582; `tennislab reproduce-small --scenario tier`, `make reproduce-tier`). The scoped C2 ports (T9, T10, ATP T11/T13), the separately repaired T2 sentinel (R28/RB16), and the runtime barrier gate run on the base scenario in `tests/`; every one fails, not skips, without its fixture, and every one is paired with a planted defect it rejects. Archive-dependent reconstruction stays optional (`tools/equivalence.py`, `TENNISLAB_ARCHIVE`). | RB9 required native tier coverage before a full_tier campaign; RB12 forbade porting a detector without its planted-defect evidence. The tier fixture measures its per-window initial-rating offsets with the package's own `tier_elo` and pins them in the chain config, so the drift refusal is exercised. The not-covered parts are named in `docs/INTEGRITY.md` §5 (T2 satellite/replay parts, T9 tier offsets on the base scenario, T11 leaderboard reconciliation). | Making the tier scenario replace the base sample's pinned number; skipping gates when the archive is absent; porting the rejected detectors (T1, T3–T8, T12) as-is. | A campaign needs a horizon or menu the fixtures do not exercise; T1 native invariance through the tier stages is added or found wanting. |

| RB16 | Repair the independently reproduced B2 enforcement gaps before integration; audit schema 2 invalidates older integrity receipts while retaining their historical evidence. Use completed read-capability logs, configured declarations, manifest-bound ledgers, closed metadata projections and runner-wide output confinement. | Different-model review of 791f020 reproduced update-mode reads, symlink stage writes and cached-verdict verification failures; root reproduced the T2 datetime64 miss and target-stat projection. Design and acceptance: `docs/B2_REVIEW.md`. No numerical settings change; fresh separate historical workspaces required. | Treating the old green suite or saved forecasts as acceptance of these integrity claims; presenting Python instrumentation as independent custody or an adversarial-process sandbox. | Any declared public-interface negative control passes unexpectedly, any final forecast differs, or a supported input/output channel changes. |

| RB17 | Native T1 is a bounded engineering check for the actual ATP tier and WTA base chains. Keep WTA tier support and full-bundle T3 open under RB9; do not add a new WTA tier model merely to close a test label. | `docs/NATIVE_T1.md` freezes a calendar-selected final-year target and strictly-future full outcome/stat intervention. Both actual tour dispatch paths, shared-base/selected/market forecasts and outcome-dependent sidecar controls are exercised. WTA market coverage is the exact pre-match priced subset. | Claiming the base WTA fixture covers nonexistent WTA tier stages; treating T1 mutation invariance as the separate T3 null experiment or as comprehensive leakage/campaign acceptance. | A future registered campaign adds WTA tier support, changes the information set, or completes full-bundle T3. |

| RB18 | Lane D live slice (2026-09-14): live results rows are dated at the declared event-window end (`completion_upper_bound`, a bound) and are eligible for a fixture only when that bound is at or before the D−2 cutoff, the captured source revision's timestamp and the acquisition receipt precede the issue time, and the row is not overlap-unresolved; serve states and rankings come only from their own feeds; only the `elo` rung issues live forecasts, every other rung writes an explicit `forecast_unavailable` record; the proof adapter is offline and hash-only. | R12/R19/RB13 forbid using an event anchor as availability evidence; the Wikimedia core REST envelope carries the revision id and timestamp, which is the only publication evidence the qualified source supplies. The trunk has no single-fixture feature route: the exact route for P0/P1/full_tier/base/full is a live-panel append and a chain replay from `features` to `pipeline` with the fixture year's labels blank, which needs a bound panel and fitted artifacts this lane did not fit. OpenTimestamps submission is a separate D30 step. | Dating live rows at the event anchor (R12 wording) or at start plus round order; a bespoke live feature builder; a live OTS client in this lane. | A qualified per-match date source (A3) changes the bound; a bound live panel and B2-accepted tier path makes the chain-replay route runnable. |

| RB19 | Repair the independently rejected Lane D slice before D2. Bound history now carries typed completion, publication, receipt, played-status and overlap fields and passes the same cutoff gates as incremental rows; the scheduled local date comes from a validated IANA timezone; version manifests hash-bind acquisition receipts; batch files are hash- and ledger-identity-bound; adapters require every consumed field; live output paths use physical symlink confinement and conservative single-segment IDs. The failed `50518be` source commit, its port `954f449`, original design/freeze and reconstruction report remain evidence. | The independent reconstruction reproduced Elo arithmetic but admitted five isolated ineligible history rows, derived Chicago's cutoff from the input offset, changed eligibility by editing a receipt, changed the forecast subject by editing fixture bytes, bypassed field qualification with a source-wide status, escaped settlement/ledger outputs and reproduced a tracked hygiene failure. The public controls frozen in `docs/live/REPAIR.md` fail on the port and pass after repair. Receipt time proves local custody only; publication evidence is the separate retrospective-availability field. No numerical setting or real forecast changed. Rebuild R32 separately blocks unconditional TML supplied-date admission after retained negative offsets contradicted the never-early summary. | Accepting the old green working-tree claim; status-only source checks; deriving local date from the timestamp's displayed offset; treating a mutable receipt, batch manifest or event anchor as availability proof; ad hoc TML date padding. | Any public negative control unexpectedly passes, a bound row lacks the typed fields, a source/date qualification changes, or D2 provides corroborated TML completion bounds under a separately recorded rule. |

| RB20 | A second targeted Lane D repair is required before D2 or integration acceptance. Fixture qualification binds the exact version-manifest SHA-256 as well as its ID, and every forecast load rechecks that digest. An explicit settlement version requires the unchanged latest-pointer digest or a chain-verified fixture qualification for that version. Forecast/proof leaves pass physical confinement and use exclusive creation. Shared JSON/CSV writers use standard-library unpredictable, exclusive same-directory temporary files before the existing atomic replace and refuse final-leaf symlinks. Malformed timestamp text becomes the existing typed fixture exclusion. | Independent reconstruction rejected `542d85e` after coherently rehashing an edited receipt in the mutable version manifest changed the accepted forecast, after explicit `settle --version` accepted the same changed version, and after dangling forecast/proof leaves plus the predictable `latest.json.tmp.<pid>` escaped the live workspace. The new public controls are frozen in `docs/live/REPAIR2.md`; they reproduce all six manifestations on the rejected baseline. The change to `chain/common.py` is limited to temporary-file creation and leaf refusal: serialization, fsync, replace, returned digest, caller signatures and numerical logic are unchanged. A digest proves bytes stayed unchanged since trust assignment, not that their initial contents are true. | Rehashing a mutable manifest as a new trust assignment; adding a general security framework or claiming hostile-process sandboxing; changing model settings or broad shared-chain behavior. | Independent review of the exact new commit fails any affected control or clean-clone check, a valid existing writer changes bytes/caller behavior, or the trust-assignment model changes. |

| RB21 | Resolve every settlement version through one precedence rule: first verify and collect hash-chained qualification digests for that exact version ID; multiple distinct qualification digests refuse; one qualification digest is the prior anchor and must agree with a matching current pointer; only a version with no qualification anchor may rely on its matching current pointer. Pass that resolved digest to version loading on both explicit and default settlement paths. | Independent repair2 review closed F2/F3 and the original F1 forecast/older-version cases but rejected `4295df4` on ATP and WTA: after qualification, coherently rehashing the receipt, version manifest and mutable latest pointer left the ledger and fixture unchanged; forecast refused, while explicit current and default settlement both returned success. `_explicit_version_manifest_binding` returned the pointer before reading the ledger, and default settlement discarded the expected digest after `latest_version`. Public controls frozen in `docs/live/REPAIR3.md` reproduce both bypasses and the multiple-prior-anchor case while preserving unqualified/new-current versions. | An explicit-only caller patch; treating the mutable pointer as a replacement for an earlier qualification; assigning an old version's anchor to a new version ID; another provenance framework. | Independent review of the exact repaired commit fails a resolver conflict/positive control, or the version trust model changes. |

| RB22 | Accept manual-workflow repair `5089b24fe29e351d6f5f1c18fc970038b9c2c3cb` for integration within its synthetic scope. | Independent repair3 reconstruction: 36/36 controls and 62 affected tests passed; original receipt/manifest/pointer conflicts, write escapes and malformed timestamps covered. Review SHA-256 `8afad3ada6a8d6b014a961a100f20321e02854baaf0ab089d220551044a37e6c`. Earlier full check at `4295df4` reused only for unchanged code; final merged CI remains separate. | Treating builder checks as independent acceptance; claiming D2 or prospective evidence from synthetic Elo. | An affected invariant fails or the real-source/forecast scope changes. |
| RB23 | Preserve Git history with the owner's 2026-09-14 one-file exception for historical `tests/fixtures/solver_precision_2005-06-25.json`; current tests use an independent synthetic replacement. | Exact former-file hash, original commit, source and terms are recorded in `DATA_LICENSES.md`; owner explicitly selected preservation with a documented exception. | Rewriting public history; broad permission to redistribute archive rows; treating source-derived data as MIT code. | Another source-row artifact is found, source terms require a change, or the owner changes the publication boundary. |

| RB24 | Accept WTA01 historical binding `87448ab` and focused audit successor `41b8204` for retrospective input use. | Independent reconstruction matched 288 forecast files, 10 reports and 180 training-key sets; 12,900 primary and 12,785 priced keys independently verified. Audit successor closes altered fit-metadata and false design-commit admission, validates actual fitted state, full inventory and 18-stage ledger on the retained run. See `docs/equivalence/WTA01-binding_attempt_002/README.md`. | Substituting WTA02; presenting outcome-assisted rules, constant 2016 dynamic training or exposed cohorts as prospective evidence; treating the audit as a general future-run scientific gate. | A bound artifact or contract changes; E/G acceptance and empirical freeze remain separate. |

| RB25 | Replace the platform-sensitive single synthetic numerical fixture with a bounded PCG64 seed bank; choose solely from legacy failure and independent BFGS qualification, then test repaired public apply_batch once. | The single-seed assertion failed on Linux at bb15fac and cf05438. Focused successor c985c65 passes macOS (seed89) and hosted Ubuntu24.04/Python3.14.6 run34909464749 (seed351): both legacy nonconvergence above original gradient/decrement limits, current3 iterations, zero reported objective difference and parameter errors below3.3e-14. | Skipping the legacy negative control; loosening solver/comparison tolerances; choosing a seed using repaired-solver success; restoring real data. | No qualifying case exists within the fixed bank, the repaired public solver fails, or independent comparison fails. Final integrated full CI is separate. |

Current B2 disposition (2026-09-14): the scoped RB11 integration prerequisite is closed
by independent enforcement review, fresh historical reconstruction, native ATP tier/WTA
base T1 checks and the passing full `make check` recorded in `docs/B2_REVIEW.md`. The
original RB11 warning remains as history. This does not close RB9: WTA tier support and
full-bundle T3 remain open, and the other unqualified C2 detectors remain unqualified.
The archive rebuild directory contains `LANE_B_acceptance.md` and the original briefs.

| RB26 | Distribute the full accepted trained model collection separately from private source data, through a versioned research release. | Owner explicitly requested full capability. Candidate 2924937 and tar bbe0337006e298897f6dda0ed87f0717d8a451e465235d95078916def74bcfc4 preserve 40 selected models, 40 fit manifests and Elo state bytewise; all 40 HGB public interfaces and corrected source-ID Elo behavior verified. See docs/MODEL_RELEASE.md for exact coverage, terms and test scope. | Publishing only a weaker Elo substitute; claiming prepared-feature inference is a complete player-name app; redistributing raw source rows; promoting pending E/G candidates. | An included artifact or source term changes, a loader/parity check fails, or the owner authorizes a new model version. Integrated CI must pass before the public asset is released. |
