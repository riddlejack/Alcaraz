# Independent final T1 comparator and claim review

Reviewed repairs: README wording in `77a4afd`; comparator and controls in `50d9fba`. No tracked code or documentation edited by the reviewer.

**Verdict: the two material final-pass findings are closed.** README now distinguishes in-memory selection scoring before the barrier from recomputation/publication afterward. The comparator examines all chosen target IDs before enforcing the exact expected sports or priced-market subset, rejects duplicates, and compares complete rows. It checks each emitted forecast CSV under the frozen target year, rather than skipping files with no target rows. Earlier-year files are outside this target-date experiment; the market `selection_keys.csv` exclusion is appropriate because it records past calibration membership, not forecasts. Clean/mutated file inventories must still match.

Independent replay of the original tiny full-tree reproduction now raises `target coverage` for the extra chosen-but-unpriced market row that previously passed. A separate tiny-tree script exercised four cases through real `compare_runs`, without mocks: extra unpriced market target, missing priced target, duplicate target, and an additional final-year raw forecast file empty in both clean and mutated trees while another raw file remains populated. Each healthy tree passed first and each mutation was then rejected. The same positive trees included an earlier-year forecast and the target-year market selection-key receipt, verifying that their intentional exclusions still work. `uv run pytest -q tests/test_native_t1.py -k 'comparator'` independently passed all four committed controls.

The actual native-run counts remain the integrating owner's separate evidence: ATP 4 targets / 30 artifacts / 120 rows; WTA 4 sports targets, 2 priced targets / 14 artifacts / 52 rows. The comparator repair makes those coverage assertions enforceable per emitted target-year forecast. It does not add WTA tier support, the separate T3 null experiment, general leakage proof, or campaign acceptance. R28's repaired T2 scope and the historical value-versus-provenance distinctions remain appropriately bounded in the reviewed docs.

Retained independent reproduction scripts: `independent_t1_comparator_baseline.py` and `independent_t1_comparator_followup.py`. The full integrated suite and fresh historical numerical reconstructions are owned separately by the integrating owner; this addendum does not claim their completion.
