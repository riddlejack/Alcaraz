# Independent B2 follow-up: 89de382 plus corrective 907b79e

Reviewer: Codex independent subagent; no tracked code or archive edits. This follows the read-only baseline review of Fable's 791f020 in `INDEPENDENT_REVIEW_791f020.md`. The integrating owner implemented repairs. Review scope: runtime outcome access, metadata projection, report-barrier scanning, local verification and output confinement; additionally, independently reconstruct the original R28 T2 counterexample. Historical numerical reconstruction, native T1 and broad campaign acceptance remain separate work.

## Corrective verdict

**The three baseline findings are closed in the reviewed scope.** A further date-horizon gap found at 89de382 was reproduced, repaired in 907b79e and independently rejected on replay. No unresolved actionable finding remains in this review's scope. This is scoped engineering acceptance of accidental forbidden file reads and local receipt consistency, not an assertion that a Python audit hook is a security sandbox, that shared-filesystem receipts are independent custody, or that the model has prospective value.

## Tests and independent reproductions

1. At 89de382, `uv run pytest -q tests/test_b2_review_regressions.py tests/test_barrier_gate.py tests/test_config.py --basetemp <REVIEW_WORKSPACE>` passed all **43 tests**: 22 new review regressions, 11 original barrier tests and 10 config tests. These include a freshly built full base synthetic chain; missing audit completion; supported read-capable modes; declarations; constrained metadata projection; nested bookkeeping-name metric artifacts; forged commitments; nine families of symlink/config destinations; cached-manifest corruption; and consistently rebound bad log horizons.
2. Independent scratch subprocesses read a real outcome-bearing CSV using `r`, `r+`, `rb+`, `a+` and `os.open(O_RDWR)`. Every read is now observed and rejected for a configured nonempty-module stage declared `none`. The original direct `run_stage` symlink reproduction now raises before writing, with the outside directory unchanged. These direct controls use neither the regression-test shims nor mocked file hooks. Retained script: `independent_followup_direct.py`.
3. At 89de382, copying the fresh SR03 log and setting both a fold's `cutoff_date` and `max_match_date_returned` to `2099-01-01` escaped the first repair (fold 2016, season ceiling 2015). The first date check compared latest date only with its own declared cutoff. The integrating owner added the consuming-fold bound in 907b79e. Replaying the identical control now reports `cutoff 2099-01-01 exceeds fold cutoff 2015-12-30`.
4. With final 907b79e code, the retained schema-2 clean run verifies: **14 stages, 14 ledger records, zero integrity problems**. Each of the committed consistently-rebound log controls was then invoked on its own copied retained workspace: (a) season ceiling reaches its fold; (b) returned date exceeds cutoff; (c) both returned date and cutoff extend beyond the fold. Outputs, manifests and ledger digests were deliberately rebound and cached verdicts left clean. All three fail the actual reconstructed horizon check. Thus rejection is not merely a hash-mismatch result. These final controls validate the small date-check delta without regenerating another numerically identical sample. Retained direct script: `independent_followup_delta.py`.

## T2 re-admission after R28

Read the original rejection in archive `docs/reviews/rebuild_2026-09-13/LANE_C2_reconstruction.md:29`: match `2005-301/20`, opaque `independent_opaque_release_clock=2005-01-13`, cutoff `2005-01-10`, dictionary `dtype=datetime64[ns]`, `role=provenance`. Independently reconstructed that counterexample through the repaired top-level `audit_dates`/`assert_date_audit`: the added field is discovered, its lag is **-3 days**, and the same assertion used for positive evidence rejects it. Moving the value to the cutoff passes, preserving inclusive cutoff semantics.

`uv run pytest -q tests/test_gate_t02_feature_dates.py -k 'r28 or non_calendar'` passed **28 cases**: eight calendar dtype spellings across three supported dictionary layouts and four explicit non-calendar exclusions. On the retained synthetic run, independently invoked the clean audit plus the new/renamed/dictionary-role/dictionary-group controls, the real-row future-date control and equal-target cutoff control. Clean result: **5,350 rows, five source-date columns, zero violations**; each promised negative was rejected.

**T2 is admissible for this repaired scope:** header/dictionary discovery using recognized date names, roles/groups and the supported declared calendar dtypes, and source-date comparison with the declared row cutoff. This does not revive the rejected universal-discovery claim. Untyped opaque columns, dates hidden in serialized text, satellite date reconstruction and numerical Elo replay remain outside these checks. The archive R28 decision is not edited by this review; the integrating owner should record this new evidence and explicit scope when admitting the repaired product gate.

## Selection and other boundaries

The baseline independent arithmetic reconstruction remains applicable: 15 sample calibration fits, six candidate decisions and three market criteria reproduced with maximum loss error 1.11e-16 and maximum optimum derivative 2.55e-14, without calling production scoring/fitting code. The reviewed changes do not modify that arithmetic. Fresh historical ATP/WTA reconstruction is owned separately by the integrating owner; no historical result or campaign gate is certified by this follow-up.

Read-only observation covers workspace file opens and recognized outcome formats. In-process dataflow, non-file channels, unrecognized schemas and wholesale coordinated replacement of all local history are outside the enforcement claim. Barrier scanning retains its documented recognized-format/key scope. Metadata projection explicitly retains its registered eligibility and resolution information; it is not physical outcome isolation. Schema-1 runs intentionally fail the new verifier and must remain historical evidence rather than being silently upgraded.
