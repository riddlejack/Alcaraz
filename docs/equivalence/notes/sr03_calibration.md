# sr03_calibration
Base revision: TIER01_models/sr03_calibrate.py (SR03 calibration.py year-parameterised by `year_plan`). WTA02 additions merged: `tour` in the `point_run_manifest` panel-binding authority; `calibration.score_years_max` scoring ceiling — when the key is declared (even null) `scoring_boundary.json` is written and only calibration years <= the ceiling are scored; when absent (TIER01) every outer year is scored and no boundary file is written. Core in `tennislab.dynamics.calibration` (`CalibrationWindow`, default = SR03's frozen window); stage in `tennislab.dynamics.calibrate`.
Bindings: `bindings.files` is read as given. Data entries (`{path, sha256}` not ending in `.py`) must be exactly the six `source.*_path` files and are verified by hash under the workspace. Code entries are declared provenance, recorded in `run_manifest.json` `code.declared_binding` and never checked against the package: accepted shapes are the archive's `{path: "...py", sha256}` with `bindings.implementation_path` (then `references/SR02_models/market.py`, `runner.py` and the implementation path must all be listed), and the chain driver's `{module, sha256, package_version}` receipts with `bindings.implementation = "tennislab.dynamics.calibrate"` (at least one receipt required).
ATP TIER01/attempt_002: identical 12/13 (start.json, fit_events.jsonl, fits.json, training_membership.csv, predictions.csv, predictions_boundary.json, metrics.csv, reliability.csv, comparisons.json, cohort_counts.json, stdout.txt, stderr.txt); differing: run_manifest.json (1 leaf: `code` block added — package receipts for calibrate/calibration/market/sr02_runner plus `declared_binding`) — provenance only.
WTA WTA02/attempt_002: see wta_sr03_calibration.md.
Label reads (all marked `# outcome-history read` in `calibration.py`; this stage is the archive's B1 exposure):
- `calibration.load_dataset` reads the panel's `a_won` for every selected match with `played = true`, every calendar year in the window (TIER01: 2011–2024, target years included), into `Dataset.outcomes`.
- History use (allowed): `training_rows` keeps rows with a resolved outcome in the three calendar years before the outer year, cut at 30 December; `fit_and_predict` fits one nonnegative slope per family on those outcomes (`market.fit(raw, outcomes, penalty=None)`). `fits.json`, `training_membership.csv` and `predictions.csv` depend on outcomes only through these prior-year fits.
- Scoring use (must move behind the report barrier): `evaluate` joins `outcomes[match_id]` onto every prediction of every outer year (2014–2024 here, i.e. the reserved target years 2017–2024) and computes `metrics.csv` (log loss and Brier via `market.scores`, per cohort × year × model, plus the priced-primary scope), `reliability.csv` (per-bin `outcome_rate`), `comparisons.json` (`sr02_runner.comparison`: paired per-match log-loss differences, annual means, t-interval and event-stratified bootstrap) and `cohort_counts.json` (row counts of the outcome-joined population). `predictions_boundary.json` records the predictions hash before scoring.
Learned constants: per outer year and family, one slope (`fits.json` `market_slope`), horizon = training years `outer_year-3 .. outer_year-1` through 30 December, membership hashes recorded per calendar year.
Open: nothing.
