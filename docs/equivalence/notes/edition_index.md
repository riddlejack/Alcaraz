# edition_index
Base revision: WTA02_models/build_edition_index.py (TIER01_models/build_edition_index.py plus `--unordered-source`, which accumulates editions per effective date and emits them in date order instead of refusing a source whose rows are not in nondecreasing date order; the WTA stream carries 3,712 order decreases, the ATP stream none, so an ATP build without the flag is the TIER01 behaviour unchanged). Ported changes: the row validator and header are imported by name from `tennislab.chronology.ranking_lookup` instead of being loaded from `work/MULTI01_ranking_lookup/ranking_lookup.py` at a pinned hash (`--lookup-module-sha256` is still accepted and, when supplied, recorded in `summary.json` as `declared_binding`; neither run supplies it, so no leaf is added); paths go through `resolve_under_root`/`relative_to_root`.
ATP TIER01/attempt_002: identical 1/4 (`stderr.txt`); differing: `edition_index.json` (1 leaf: `generated_at_utc`, a wall clock — all 1,197 editions, 11,442 `known_player_ids`, `source_path`, `source_sha256`, `source_bytes`, `source_rows`, `source_header` identical), `summary.json` (4 leaves: `generated_at_utc`; `edition_index_sha256`, which is the hash of the index whose only change is that timestamp; and the two descriptive keys the WTA02 revision adds, `source_effective_date_order_decreases: 0` and `source_order_policy: "streamed in nondecreasing source order"`), `stdout.txt` (the summary document, same four).
WTA WTA02/attempt_002: identical 1/4 (`stderr.txt`); differing: `edition_index.json` (1 leaf: `generated_at_utc`; all 1,344 editions and 8,002 player ids identical), `summary.json` (2 leaves: `generated_at_utc` and the dependent `edition_index_sha256`; the WTA archive run already carried the two order keys, and their values — 3,712 decreases, "accumulated per effective date and emitted in date order" — are reproduced), `stdout.txt` (the same two).
Label reads: none.
Learned constants: none.
Open: `edition_index.json` cannot be byte-identical while the stage stamps `generated_at_utc`. Its only differing leaf is that timestamp, which is the wall-clock provenance class `docs/EQUIVALENCE.md` names; every index row is identical. If byte identity of this file is wanted downstream, the stamp has to be dropped or made an input — that is a contract change for the integrating owner, not a port decision.

Integration (lead, decision RB6): `generated_at_utc` removed from `edition_index.json` and
`summary.json` so the artifact is deterministic; the stage manifest carries the wall clock.
After the change `edition_index.json` differs from the archive only by that absent leaf.
