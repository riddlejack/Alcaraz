# join
Base revision: TIER01_models/join_candidates.py (the WTA02_models copy of that file is byte-identical, so there is one revision and nothing to merge). The WTA tour has its own join program; see `wta_join.md`.
Module: `tennislab.panel.join` (+ `tennislab.sources.profile_annual`, the qualified annual-workbook adapter the archive loaded by path).
ATP TIER01/attempt_002: identical 10/13 (alias_candidates.csv, ambiguous_candidates.csv, ch01_validation.json, common_panel_candidates.csv, conflict_locators.csv, coverage_by_year.csv, date_correction_candidates.csv, market_row_candidates.csv, unmatched_archive_rows.csv, unmatched_market_rows.csv); differing: audit.json (`implementation.adapter` — the archive recorded the running file's repository path and hash, the port records `code_receipt("tennislab.panel.join")`; every other leaf identical, including `implementation.tests` and `implementation.original_adapter`, which stay declared bindings resolved and hashed from `references/MULTI01_inputs/MULTI01_join/`, and `inputs.market_profile_adapter`, likewise still hashed from `work/MULTI01_market_acquisition/profile_annual.py`), stdout.txt (the same audit document, so the same one field), stderr.txt (an openpyxl `UserWarning` whose text embeds the interpreter's own site-packages path — environment provenance, same warning, same line).
WTA WTA02/attempt_002: not this module — the WTA join is `tennislab.panel.wta_join`; see `wta_join.md`.
Label reads: `validate_selected` reads the panel's `a_won` orientation as history to decide `winner_agreement`; marked `# outcome-history read`. No score is computed.
Learned constants: none. The 2006/2013 source-date anomaly rules, the CH01 pins and the recovered-2018 hash stay literals — they are evidence about named rows, not measured constants.
Open: nothing.
