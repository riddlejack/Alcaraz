# prepare_panel
Base revision: WTA02_models/prepare_panel.py (WTA02 additions merged: none needed — the WTA02 copy is a strict superset of TIER01_models/prepare_panel.py. The 196-line diff is the WTA docstring, one blank line in `_resolve`, and the added `tour` switch with `build_wta`, `wta_tier`, `wta_exclusion` and their dry-run branch; the ATP integration is byte-identical between the copies.)
Module: `tennislab.panel.prepare`, serving both tours. The join is imported by name (`tennislab.panel.join`) instead of loaded by path, and its `configure()` is fed the same config document, so join and prepare cannot disagree about their inputs.
ATP TIER01/attempt_002: identical 5/7 (panel.csv, exclusions.csv, corrections.csv, coverage.csv, stdout.txt); differing: manifest.json (3 leaves — the archive hashed `prepare_panel.py` and `join_candidates.py` into the `inputs` path->sha256 map; the port keeps `inputs` a pure map of the nine data inputs and records both modules' `code_receipt`s under a new `code` key. Every other leaf identical: 51,222 rows, 49,792 primary / 1,430 provisional, the same exclusion reasons), stderr.txt (an openpyxl `UserWarning` whose text embeds the interpreter's own site-packages path — environment provenance).
WTA WTA02/attempt_002: identical 6/7 (panel.csv, exclusions.csv, corrections.csv, coverage.csv, stdout.txt, stderr.txt); differing: manifest.json (the same 2-leaf `code` substitution; 46,827 rows, 45,653 primary / 1,174 provisional, 332 `inferred_event_end` rows, all identical).
`code` is omitted from the printed one-line summary, as `inputs`, `outputs` and `upstream` already were, so stdout.txt stays the archive's bytes.
Label reads: the price orientation reads the panel's `a_won` as history to name `PS_decimal_a`/`_b`; marked `# outcome-history read` on both branches. `qualify`/`wta_tier` decide the identity tier before any outcome field is consulted.
Learned constants: none measured here. `additional_guarded_RG_corrections: 6` and `resolved_ambiguity_rows: 279` stay literals — they are MULTI01 receipts (6 corrected count *fields* across 2 guarded RG cases; 279 accepted of 283 resolution rows read) and carry their derived siblings `rg_cases_verified` and `resolution_rows_read`.
Open: nothing.
