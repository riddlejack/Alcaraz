# tier_stream
Base revision: TIER01_models/tier_stream.py (revision 2, satellite-circuit dating; no WTA02 copy exists — the stage is ATP-only). Ported changes: `ROOT`-relative strings become `relative_to_root` (the ARCHIVE01 manifest pin check and the `inputs` receipts); `code` is `code_receipt(__name__)`; the circuit rule calls `tennislab.chronology.dating.satellite_circuit_end(min(anchors), legs)` in place of the inline `anchor + offset_days × legs`, so `satellite_circuit_dating: true` now requires `reported_date_offset_days == DAYS_PER_CIRCUIT_LEG` (7) and refuses the 0-day sensitivity offset in that mode (a combination no archive run used; `false` + 0 still reproduces revision 1's sensitivity). The build is split into `read_parameters`, `verify_custody`, `build` and `dry_run`; the writer is the archive's `gzip.compress(payload, mtime=0)` (Python 3.14.6 on both sides, `zlib` gzip header) and `atomic_csv`. The `rule`/`basis`/`limits` strings in `summary.json` are the archive's verbatim.
ATP TIER01/attempt_002: identical 7/8 — `tier_results.csv.gz`, `tier_source_rows.csv.gz`, `tier_circuit_dating.csv`, `tier_counts_by_year.csv`, `tier_exclusions.csv`, `stdout.txt`, `stderr.txt`; differing: `summary.json` (4 leaves, all under `code`: `path`/`sha256` of the archive program become `module`/`sha256`/`package_version` of the package module).
WTA WTA02/attempt_002: n/a (no WTA stage).
Reserved window: a `last_year` of 2025 or later is refused (`refusing to read a reserved year: …` in `build`, `configured span reaches a reserved year` in `--dry-run`) unless the `tier_stream` section carries `reserved_release_acknowledged: true`, the bridge's acknowledgement. `chain.runner` `write-configs` copies the chain config's own `reserved_release_acknowledged` into this config only when it is `true`, so one chain-level acknowledgement covers both stages and a non-acknowledging chain emits the same config as before. When a reserved year is opened, `summary.span` adds `reserved_years_opened` and `reserved_release_acknowledged`, `reserved_years_never_opened` keeps only the unopened ones, and the Futures limit sentence states that the 1991–2024 audit does not cover the opened years and that a Futures row there carrying a complete serve block meets the count-identity screen (failing rows are dropped from the outcome stream and listed in `count_identity_failure_rows`; no Futures row enters the SR02 feed). A span ending in 2024 or earlier is byte-identical to before in every output except `summary.json` `code.sha256`.
Surface case: a stripped surface is matched to `SURFACES` up to case (`SURFACE_BY_CASEFOLD`), so the 2025 Futures file's lowercase `clay`/`carpet` rows are mapped to `Clay`/`Carpet` instead of being excluded as `surface_unsupported`; a surface still unknown after case folding is excluded as before. `surfaces_seen` keeps the source spelling; `summary.surface_case_normalized_rows` (family → season → rows) and a closing limits sentence appear only when a row was mapped. No 1991–2024 row of either family is a case variant (surface column of the pinned ARCHIVE01 tarball: 201,697 qualifying/Challenger and 498,555 Futures rows, all exactly `Hard`/`Clay`/`Grass`/`Carpet`), so spans ending in 2024 or earlier are byte-identical except `summary.json` `code.sha256`.
Label reads: none. The outcome stream carries `winner_id`/`loser_id` as a source column (the same bytes the Elo replay needs); the SR02 feed carries `SOURCE_ROW_FIELDS` only and cannot see a winner. Nothing is scored.
Learned constants: none. The reported-date offset (7) and the leg length are declared, not measured.
Dating: every row is written with `anchor_date` (the archive's `tourney_date`) and `date` (the reported bound); for a satellite component edition the bound is `satellite_circuit_end` — Spain 1 2006 (`2006-M-SA-ESP-01A-2006a/b/c/d`, anchor 2006-02-27, four legs) is dated 2006-03-27 on all 115 retained rows, recorded in `tier_circuit_dating.csv` and pinned end to end in `tests/test_ratings_tier_stream.py` on a synthetic ARCHIVE01 tarball (the archive's own test skipped unless its corrected stream was on disk).
Tests: `tests/test_ratings_tier_stream.py` (28): the eight MULTI01 identities, block status labels, the score vocabulary, the audit's tier split, the undeclared-offset / reserved-year / circuit-with-zero-offset refusals, the reserved-window acknowledgement (both refusal messages unchanged without a literal `true`; an acknowledged 2024–2025 build and what its summary says it opened; a span ending in 2024 unchanged with or without the flag, and its rows equal to the 2024 rows of the 2025 build), surface case drift (lowercase surfaces mapped, counted, named and fed as canonical; unknown surfaces still excluded; no field or sentence without drift; no other row moves), the leg-key shape, the Spain 1 2006 dating (four legs → 2006-03-27), one-leg and two-leg circuits, the end-to-end build (redating, the challenger's anchor + 7, the SR02 feed's usable/missing_all labelling, the exclusion counters, the nonempty-output refusal) and the manifest pin check.
Open: nothing.
