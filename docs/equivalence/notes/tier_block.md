# tier_block
Base revision: TIER01_models/tier_block.py (revision 2, with the `tier_noqual_dynamic` ablation columns; no WTA02 copy exists — the stage is ATP-only). Ported changes: `load_module(CODE_DIR / "runner.py")` becomes `from tennislab.models import pipeline`, and `verify_forbidden(dictionary_path, *, noqual)` drops its module argument (it installs the tier bundles on the package module's state, as the archive did on the loaded runner, and cross-checks `TIER_SIGNED`, `TIER_DYNAMIC_SIGNED`, `TIER_PROVENANCE_ONLY_COLUMNS`, `TIER_NOQUAL_DYNAMIC_SIGNED` and `TIER_NOQUAL_PROVENANCE_ONLY_COLUMNS` against it); the per-row join is `tier_block_row` and the panel history `tour_history`; `ROOT`-relative strings become `relative_to_root`; `code` carries `code_receipt` for this module and for `tennislab.models.pipeline`. The writers are the archive's (`atomic_csv`; `gzip.compress(payload, mtime=0)` through `tempfile.mkstemp`).
ATP TIER01/attempt_002: identical 7/8 — `trait_latent_sidecar.csv` (51,222 rows, 46 MB), `tier_detail.csv.gz`, `tier_dictionary.json`, `coverage_by_year.csv`, `thin_side_membership.csv`, `stdout.txt` (which prints the sidecar hash `fbff9510…`), `stderr.txt`; differing: `summary.json` (4 leaves, all under `code`: the archive's `path`/`sha256` become `code.tier_block` and `code.runner` receipts).
WTA WTA02/attempt_002: n/a (no WTA stage).
Label reads: none. The stage reads the corrected panel (five identity/chronology fields), the sidecar, `tier_elo_features.csv` and the two SR02 replay selections; `labels.csv` is not opened and no outcome column exists on any input it reads.
Learned constants: none. The lag (2) and the thin-side threshold (20) are declared and refused if changed; `tier_prior_tour_matches_{a,b}` are counts at the D-2 cutoff, not fitted.
Forbidden guard: `verify_forbidden` recomputes the pipeline's model column set from the base dictionary with the tier bundles installed (231 columns, 26 signed in `base_tier`, 35 in `full_tier` and `full_tier_noqual`) and refuses if any of the 20 (+3 ablation) provenance columns reached it or is missing from `FORBIDDEN_MODEL_COLUMNS`; `summary.forbidden_check` is identical to the archive's.
Tests: `tests/test_features_tier_block.py` (11): the three column-tuple cross-checks against `tennislab.models.pipeline` (plus the probability names and the ablation tuples), no model column forbidden and every provenance column forbidden, every column the block reads from `tier_elo` is in `tier_elo.FEATURE_FIELDS`, the forbidden guard on a 162/22/6 dictionary with and without the ablation and its refusal of a provenance column in a model list, the bisect history at the cutoff, the audit's no-lag rule, the end-to-end join (column order, capped signed differences, membership, D-2 tour counts, thin-side membership, the dictionary), the no-ablation shape, the membership refusals and the fixed lag/threshold. The archive's `test_the_forbidden_guard_passes_on_the_real_dictionary` ran against `work/MULTI01_features/…/column_dictionary.json`, which is not in this repository; the harness run above is the real-dictionary check.
Open: nothing.
