# bridge
Base revision: WTA02_models/bridge_res2026.py (the superset; the 548-line diff against TIER01_models/bridge_res2026.py is the `tour` switch, the WTA readers/manifest/identity, `unresolved_event_policy`, the event-end dating with the infobox reader, and three provenance columns — every TIER01 line survives under `tour = "ATP"`, nothing TIER01-specific to merge back). `crosswalk_v2.py` is byte-identical in TIER01 and WTA02 and ports to `tennislab.panel.crosswalk_v2`. `market_event_names.json` (identical in CONFIRM2026/TIER01/WTA02) and `WTA02_models/wta_market_event_names.json` are copied byte-for-byte as package data beside the module (sha256 `335dfe31…` and `c2bc7858…`, equal to the archive's).
ATP TIER01/attempt_002: stage outputs identical 2/2 (`stdout.txt`, `stderr.txt`). Inputs tree identical 5/8 (`market/acquisition_manifest.json`, `quarantine/dropped_duplicates.csv`, `quarantine/unmapped_events.csv`, `quarantine/unmatched_players.csv`, `sackmann/atp_matches_2024.csv`); differing:
- `archive/atp_panel_source.tar.gz` — the 4 gzip-header mtime bytes only: same size, bytes 0–3 and 8–end identical, gunzipped stream identical. The archive wrote it with `tarfile.open(..., "w:gz")`, whose gzip header carries the wall clock (its own bytes were not reproducible run to run); the port fixes that mtime at 0 (member headers were already mtime 0 / uid 0 / mode 0644 / no owner names, kept as-is). Consequence: `bridge_summary.json` `composed_archive.sha256`.
- `bridge_summary.json` — 58 leaves, all provenance or WTA02-revision additions: `code.*` (sha256 strings → `code_receipt` records; `code.wta_sources` null under ATP), `market_event_names.path` (`references/CONFIRM2026_models/…` → `tennislab/sources/market_event_names.json`; sha256 unchanged), `composed_archive.sha256` (above), `outputs.market/market_row_provenance.csv` (below), and the WTA02 revision's descriptive additions that TIER01's own copy did not write: per draw `generated_tourney_id`, `tournament_end_date`, `event_end_basis`, `infobox_date_text`, `infobox_html_sha256`, `week_starts_in_cell` (6 × 8 draws), `unresolved_event_policy` (top level and in `res2026_event_resolution`, both `"refuse"`), and the `limits[1]` sentence. Every shared leaf identical.
- `market/market_row_provenance.csv` — the WTA02 revision's three added columns `market_date_basis`, `market_date`, `round_order` (all 2,703 rows `tennis_data_reported_date`, the workbook's own date, blank order); the archive's 8 columns are identical on every row. This run synthesized no rows, so the dating change below moves no ATP byte.
WTA WTA02/attempt_002: see `wta_bridge.md` (stage outputs identical 2/2; inputs tree identical 6/10; the four differences are the same gzip mtime, the workbook's openpyxl timestamps, and their hashes).
Extra evidence: `--self-test-year 2024` on the TIER01 config against the real mirror — `PASS`, 20/20 sampled rows identical, 19 draws / 1,022 rows fed, 1,021 regenerated and all dropped as duplicates, 2 differing only in the abandoned-set `0-0` rendering, 0 unmatched players, 0 unmapped events. Tests: `tests/test_bridge.py` (37) and `tests/test_crosswalk_v2.py` (15), including a full `build` over a synthetic tarball whose second run reproduces every output byte.
Chronology: `synthesize_market_rows` dates every synthesized row at the draw's event end (`event_end_dates`, measured per draw by `generate_from_wikipedia`: the retained page's infobox completion date when on/after the anchor, else last week start + 7). The archive's WTA02 copy still kept the older start-plus-round-order route as the function's default when no end dates were passed; its driver never took it, and the port removes it — a synthesized row with no end date is refused (`no_event_end_date`, as the archive already did), `market_date_basis` is always `DateBasis.INFERRED_EVENT_END` (`"inferred_event_end"`), tennis-data rows carry `DateBasis.REPORTED_MATCH_DATE` (`"tennis_data_reported_date"`), and `round_order` survives only as a provenance column. `tests/test_bridge.py::test_round_order_is_provenance_only` pins that neither the string `tournament_start_plus_round_order` nor `START_PLUS_ROUND_ORDER` occurs in the module. The `date_basis` strings written are the archive's own.
Label reads: `read_mirror_annual` (annual winner/loser rows, composed into the archive and used for the dedupe), `read_wikipedia` (draw-page winners/losers), `read_tennis_data` / `read_wta_workbook` (workbook winner/loser columns passed through) — each marked `# outcome-history read`; nothing is scored. Under `--allow-reserved-years` with `reserved_release_acknowledged` this stage opens the 2025/2026 files and is the chain's first outcome-access event, as the archive's stage note records.
Learned constants: none (the event-name aliases and the RES2026 event list are the archive's declared tables, copied verbatim).
Open:
- `wta_sources` readers: `tennislab.sources.wta_sources` (the WTA join group's module) did not exist, so `_wta_read_xlsx`, `_wta_as_date`, `_wta_blank` and `wta_norm_name` are copied verbatim from `references/WTA01_event_map/wta_sources.py` into `bridge.py` (block marked in the source); replace them with imports once that module lands. `code.wta_sources` records this module's receipt with a `readers` note instead of the archive's file hash.
- `tennislab.panel.join` is imported lazily by name (`join_module()`), only under ATP; its `read_market_workbook(path, year)` no longer takes the archive's `profile` argument. The archive's byte-identity check of the working join copy against the archival copy is dropped (rule 2).
- `wta_market_event_names.json` is not in PORTING.md's table; it is the bridge's own data file (loaded from `CODE_DIR` in the archive) and is added beside `market_event_names.json` — the only file written outside the literal list in the brief.
- ATP provenance CSV schema: under `tour = "ATP"` the port writes the WTA02 revision's 11 columns, not TIER01's 8. Suppressing the three under ATP would make the ATP bytes identical to TIER01's run but diverge from the WTA02 program's own ATP behaviour; left as the WTA02 revision has it, for the integrator to decide.
- Timestamp normalization: the composed tarball's gzip mtime is 0; `write_market_workbook` fixes ZIP entry times and the created property at `WORKBOOK_EPOCH`. **B2 correction:** openpyxl resets the modified property during save, despite its earlier assignment, so the workbook is not strictly byte-deterministic. The independent fresh run found only `docProps/core.xml` changed inside the workbook and only the bound `market_source_sha256` column changed in downstream market/panel tables. This remains an explicit RB7 provenance difference; cell and forecast values are unchanged.
- Harness: the shared `data/runs/equivalence/<run>` workspace was re-prepared by a concurrent harness run mid-way through this stage's first run (its `inputs/` became a link into the archive; the archive was not written to — checked by timestamps and hashes). `tools/equivalence.py` since gained per-stage workspace roots; the runs recorded in `docs/equivalence/*/bridge.json` were produced by driving the harness's own `prepare`/`run_stage`/`compare` with a private workspace root (scratch script, not in the repository). `tools/equivalence.py` was not edited.
- `--event-self-check` on the WTA02 config is refused (`carry_forward_years` carries 2025 and that route runs with reserved years disallowed) — the archive's own guard, kept.
