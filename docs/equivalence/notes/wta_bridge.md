# wta_bridge (stage `bridge`, `tour = "WTA"`)
Base revision: WTA02_models/bridge_res2026.py, the same module as `bridge.md` (`tennislab.sources.bridge`); the WTA half is the `tour` switch that revision added: the `wta_sources` workbook readers, `wta_market_event_names.json` for market-side identity, `WTA`/`Tier` columns on synthesized rows, the WTAODDS01-shaped manifest, the location-keyed round-cover index, `unresolved_event_policy = "quarantine"`, and the event-end dating with the infobox reader (`infobox_html_dir`).
ATP TIER01/attempt_002: see `bridge.md`.
WTA WTA02/attempt_002: stage outputs identical 2/2 (`stdout.txt`, `stderr.txt`). Inputs tree identical 6/10 (`market/market_row_provenance.csv` — 4,600 rows, 4,236 `tennis_data_reported_date` passthrough + 364 `inferred_event_end` synthesized, byte-identical; `quarantine/dropped_duplicates.csv`; `quarantine/unmapped_events.csv`; `quarantine/unmatched_players.csv`; `sackmann/wta_matches_2025.csv`; `sackmann/wta_matches_2026.csv`); differing:
- `archive/wta_panel_source.tar.gz` — the 4 gzip-header mtime bytes only (same size; gunzipped stream identical; the port fixes the gzip mtime at 0, see `bridge.md`). Consequence: `bridge_summary.json` `composed_archive.sha256`.
- `market/2026.xlsx` — `docProps/core.xml` only (openpyxl's `dcterms:created`/`modified`, the archive's carrying its write time) plus the zip entries' own timestamps; `xl/worksheets/sheet1.xml`, `xl/styles.xml`, `xl/workbook.xml`, `docProps/app.xml` and every other member are byte-identical. The port fixes both at `WORKBOOK_EPOCH` (1980-01-01) so the workbook's bytes are a function of its cells. Consequences: the workbook's `sha256`/`bytes` in `market/acquisition_manifest.json` (`records[20]`, the 2026 bridge record — the only two leaves that differ there; WTAODDS01's 20 records and the 2025 record carried unchanged) and `seasons_detail.2026.market_workbook.sha256` in the summary.
- `bridge_summary.json` — 7 leaves: `code.bridge`, `code.crosswalk_v2`, `code.wta_sources` (receipts; `wta_sources` names this module with a `readers` note because the readers are inlined), `market_event_names.path` (`references/WTA02_models/wta_market_event_names.json` → `tennislab/sources/wta_market_event_names.json`; sha256 `c2bc7858…` unchanged), `composed_archive.sha256`, `outputs.market/acquisition_manifest.json` and `seasons_detail.2026.market_workbook.sha256` (the two workbook consequences). Every other leaf identical, including all 16 per-draw `event_resolutions_per_season.2026` records (`tournament_end_date`, `infobox_html_sha256`, `event_end_basis`: 10 `retained_page_infobox_date`, 6 `last_week_start_plus_7_days:infobox_unusable`), `quarantine_counts` (25 unmatched name rows / 9 distinct, 123 duplicates / 97 identical, 2 events quarantined `no_mirror_edition_for_event`), `draw_structure` and `res2026_event_resolution` (`PASS_WITH_UNRESOLVED_QUARANTINED`).
Chronology: all 364 synthesized rows are dated at the event end with `market_date_basis = inferred_event_end`, as the archive wrote them; no synthesized row is dated at start plus round order (that route no longer exists in the module; `round_order` is provenance only). The 2025 season had no draw files and its retained workbook is carried unchanged (`retained_workbook_carried_unchanged_no_synthesized_rows`).
Label reads: as `bridge.md` — the WTA 2025/2026 mirror members, the 18 RES2026 draw CSVs and the two RES2026 WTA workbooks are opened under `--allow-reserved-years` + `reserved_release_acknowledged: true`; this is the WTA chain's first outcome-access event (archive stage note); nothing is scored.
Learned constants: none.
Open: as `bridge.md` — the inlined `wta_sources` readers to be replaced by `tennislab.sources.wta_sources` imports when that module lands; `wta_market_event_names.json` added as package data; the two timestamp-only reproducibility fixes. `--event-self-check` on this config is refused by the archive's own reserved-year guard (2025 in `carry_forward_years`), kept as-is.
