# reporting_config
Base revision: TIER01_models/build_config.py `reporting` (`--primary-contrast`, repeatable `--secondary-contrast`, `--bootstrap-seed`) with WTA02's additions merged under the tour switch: the reporter's identity and tour inherited from the predictor config, its `cohort`, and the `reporting_settings` block (`primary_contrast`, `bootstrap_seed`, `bootstrap_unit`) the predictor config carried from the year plan. Command-line values override the carried settings; absent both, the bundle list implies them. Module: `tennislab.chain.configs`; the reporter it binds is `tennislab.evaluation.report` imported by name.
ATP TIER01/attempt_002: identical 1/2 (stderr.txt); differing: stdout.txt (`sha256` of the written config, which carries the timestamp). Side tree `reporting.2017_2024.json`: 4 leaves — `code.reporter_path` (archive file path -> module name), `code.reporter_sha256` (archive file hash -> package file hash), `code.package_version` (added), `frozen_at_utc`. `settings` (primary `full_tier_minus_full`, secondary `base_tier_minus_base`, seed 20260912, `t_critical_95` 2.364624251592784 from the pinned scipy), `expected_membership` (18,972 / 18,882) and every input binding are identical.
WTA WTA02/attempt_002: identical 1/2; side tree `reporting.json`: the same 4 leaves. `settings` under the tour contract (`bootstrap_unit` match, both units emitted, `tour`, `contrasts`, `cohort`, seed 20260912, `t_critical_95` 12.706204736174694) and `expected_membership` (4,296 / 2,344) identical.
Label reads: none (the priced count is the sum of the saved market forecasts' `target_rows`; nothing is scored).
Learned constants: none.
Open: nothing. Two checks were added beyond the archive: the selection ledger's `config_sha256` must equal the hash of the predictor config being bound, and every recorded digest (`config_sha256`, `raw_complete_sha256`, the design and file bindings) must be a real sha256 and not the hash of empty content (`require_nonempty_digest`, B5).
