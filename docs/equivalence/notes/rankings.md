# rankings
Base revision: WTA02_models/qualify_rankings.py (superset of TIER01_models/qualify_rankings.py; TIER01's literal `atp/` member names become the `ATP` tour profile, `WTA` adds the five-column ranking header with the unread `tours` field and drops `matches_data_dictionary.txt`, and `qualification.json` gains a `tour` key). Row validation, duplicate policy, identity policy, the retrospective join and the 14-day staleness rule are unchanged. Ported changes: the `WINDOW`/`OUT_DIR`/`RANKING_MEMBERS`/`TOUR` globals and their `configure_window`/`configure_tour` installers become one frozen `Selection` threaded through the pure functions; paths go through `resolve_under_root`/`relative_to_root` instead of a root derived from the source file; `ValueError`/`FileExistsError` become `ChainError`.
ATP TIER01/attempt_002: identical 36/38 (all 28 `raw/` extracted members, `rankings_2000_2024.csv.gz`, `extraction_manifest.json`, `annual_rankings.csv`, `annual_results_join.csv`, `ranking_anomalies.csv`, `identity_conflicts.csv`, `result_rank_join_anomalies.csv`, `stderr.txt`); differing: `qualification.json` (one added leaf, `tour: "ATP"`), `stdout.txt` (the same one line; stdout is the qualification document) — the WTA02 revision records the tour unconditionally and the TIER01 revision had no such key.
WTA WTA02/attempt_002: identical 38/38 (all 28 `raw/` members, `rankings_2000_2026.csv.gz`, every summary CSV, `qualification.json`, `stdout.txt`, `stderr.txt`); differing: none.
Label reads: `<tour>_matches_<year>.csv` winner/loser ids are read to build the result-appearance population the ranking join is qualified against (`read_results`, marked `# outcome-history read`). No outcome is scored and no label column is written.
Learned constants: none. The 14-day staleness rule and the `CURRENT_MEMBER_FROM_YEAR = 2026` decade-file boundary are declared constants carried from the archive, not measured here; the coverage limitation they imply is recorded in `qualification.json`.
Open: nothing.
