# wta_join (stage `join`, WTA)
Base revision: WTA02_models/wta_market_join.py. There is no TIER01 counterpart of this file, so nothing was merged: the ATP tour has its own join program (`tennislab.panel.join`, see `join.md`), because the ATP join's evidence — CH01's player and event crosswalks, MULTI01's alias-context file, the recovered 2018 workbook, two named source-date anomaly rules — does not exist for the WTA side, which instead has a frozen, separately verified event-edition crosswalk.
Module: `tennislab.panel.wta_join` (+ `tennislab.sources.wta_sources`, ported from `references/WTA01_event_map/wta_sources.py`; its absolute `ROOT`/`TD_BASE`/`ARCHIVE_TAR` become workspace-relative constants resolved through `resolve_under_root`, and `load_mirror_season`'s mutable default-argument cache becomes a module-level dict. Every normalizer, loader, surname class and round-depth rule is unchanged.)
ATP TIER01/attempt_002: not this module — see `join.md`.
WTA WTA02/attempt_002: identical 4/6 (market_rows.csv, mirror_rows_without_market.csv, surname_link_check.csv, stdout.txt); differing: join_manifest.json (4 leaves — `inputs.code` was the archive file's path and hash and is now `code_receipt("tennislab.panel.wta_join")`. `inputs.sources_module` is unchanged: the config's `references/WTA01_event_map/wta_sources.py` binding is still resolved and hash-verified, per porting rule 2, while the code that runs is the package module. Every data leaf identical, including the frozen-artifact agreement tests: `orientation_conflicts_agree` true, the 13 frozen conflicts reproduced, no observed-only or frozen-only surname link), stderr.txt (an openpyxl `UserWarning` whose text embeds the interpreter's own site-packages path — environment provenance).
Label reads: `pair_edition` reads the mirror's `a_won` orientation as history to build the pairing key; marked `# outcome-history read`. No score is computed and no orientation conflict is resolved — the 13 reversed-label rows are reported and excluded downstream.
Learned constants: none. The surname links are logged evidence about a key (`link_dependent`), never a player-identity claim, and every linked row is barred from the primary tier downstream.
Open: nothing.
