# tier_elo
Base revision: TIER01_models/tier_elo.py (revision 2, `offset_mode = per_training_window`; no WTA02 copy exists — the stage is ATP-only). Ported changes: `load_module(elo_engine.path)` at a pinned hash becomes `from tennislab.ratings import elo`, and the config's `elo_engine` binding is recorded under `code.declared_binding` (its hash is not checked against the package file, rule 2); `TierPooledElo` is a module-level subclass of `elo.PooledElo` (the archive built it inside `make_engine` against the loaded module — `make_engine(multipliers, offset)` is kept without the module argument); the labels read goes through the label accessor (below); `replay_with` is a module function taking its inputs explicitly instead of a closure; `ROOT`-relative strings become `relative_to_root`; `code` carries `code_receipt` for this module and for `tennislab.ratings.elo`. Arithmetic, batch order, sort keys, `repr()` float rendering and column order are untouched.
ATP TIER01/attempt_002: identical 5/6 — `tier_elo_features.csv` (51,222 rows), `tier_offsets_by_year.csv`, `tier_elo_coverage_by_year.csv`, `stdout.txt`, `stderr.txt`; differing: `summary.json` (10 leaves: `code.tier_elo.*` and `code.elo_engine.*` path/hash → module/hash/version, `code.declared_binding` added, `outcome_reads` added — the accessor receipt `[{"purpose": "elo_state_replay", "rows": 51222, "year_ceiling": 2024}]`). Nine replays, `replays: 9`, wall clock ≈ 45 s.
WTA WTA02/attempt_002: n/a (no WTA stage).
Label reads: `labels.csv` read as history at `tier_elo.tour_rows` → `EloStateHistory.selected(keys, metadata)` (a `LabelHistory` subclass declaring purpose `elo_state_replay`, year ceiling `panel_end_year`), one read of every panel row's outcome; the accessor binds the file hash and checks the seven metadata fields of every returned row against `features.csv`. The outcomes orient the tour source rows (`winner_id`/`loser_id`) that `elo.parse_results` turns into state updates (`TierPooledElo.apply_batch`, marked `# outcome-history read`); `a_won` reaches no output. A target's own outcome cannot reach its features: `elo.replay` stops the cursor at `target - lag_calendar_days` and the stage re-checks `prediction.eligible_through` on every row.
Learned constants: `lower_tier_initial_offset` by season, measured by `measure_offset` inside every chronological training boundary — season Y's offset is measured through `<max(history_floor_year, Y - training_window_years) - 1>-12-31` — and written with its receipt to `tier_offsets_by_year.csv` (`season, training_window_start, offset_measured_through, lower_tier_initial_offset, mean_gap, players_measured`): horizons 2010-12-31 … 2018-12-31, offsets −78.27569413301566 (466 players) … −135.15154755359688 (823 players), all identical to the archive and equal to the values declared in `tier_elo.filled.json` (the run refuses a declared value more than 1e-6 from the re-measurement). `tests/test_ratings_tier_elo.py::test_the_offset_receipt_precedes_every_row_it_is_applied_to` checks that `offset_measured_through < training_window_start` for every season and precedes every emitted row the offset serves; seasons before `history_floor_year` (2005–2010 in the archive plan) are neither fit rows nor targets and take the floor window's offset, which is the archive's rule and is asserted as such.
Tests: `tests/test_ratings_tier_elo.py` (24): the per-tier K multiplier, the lower-tier and tour debut initial ratings, the unknown-source refusal, parent-equality with no lower-tier row, the synthetic-panel regression at 0.0 with the stream empty, the logit change with the stream in, the D-2 cutoff day, the 300/20 caps, the debut provenance, the drifted-offset refusal, chronology (a later row changes no earlier row), the after-panel refusal, the label metadata drift refusal, the corrected circuit date entering the state, the training-window horizon rules (2014–2016 read through 2010), the unknown-mode and per-window undeclared/drifted refusals and the offset receipt.
Open: `tennislab.chain.labels.PURPOSES` declares only the pipeline's three purposes (`training_fit`, `past_selection_calibration`, `past_market_calibration`) and `LabelHistory.__init__` refuses any other, so this stage declares its purpose locally: `EloStateHistory(LabelHistory)` sets the same attributes (`path`, `expected_sha256`, `purpose = "elo_state_replay"`, `year_ceiling`, `reads`) and inherits `selected` unchanged. The models group should add `"elo_state_replay"` to `PURPOSES`, after which `EloStateHistory` collapses to `LabelHistory(..., purpose=ELO_STATE_PURPOSE, year_ceiling=...)` (one-line change here). The year ceiling is `panel_end_year` because the Elo state legitimately consumes every season's outcomes up to the D-2 cursor; the ceiling protects only against a label file reaching past the panel.
