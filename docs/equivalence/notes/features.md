# features
Base revision: WTA02_models/build_features.py (TIER01_models/build_features.py plus one line in `CountHistory.player_summary`: the assertion that serve and return count history always arrive together becomes a per-domain divergence counter, and `missing_{domain}` becomes `serve_den == 0 and return_den == 0`. A match in which one side serves zero points gives that side a return observation and no serve observation; on the ATP panel the two expressions agree on every summary call, which the run below confirms — `diverged_serve_return_history_summaries` is empty and `features.csv` is byte-identical). Ported changes: the divergence counter is an attribute of `CountHistory` threaded from `build` instead of a module global; `resolve_repo_path` and the `OUTPUT_ROOT = ROOT / "work"` containment check become `resolve_under_root` (output must lie under the workspace); the ranking lookup is imported from `tennislab.chronology.ranking_lookup` instead of being loaded from `work/` via `sys.path`, with the config's `ranking_lookup_module` path+hash read and recorded in the manifest as `declared_binding`; `executed_builder` is `code_receipt(__name__)`; the label writer is the separate `write_labels`, fed the targets in the order the feature pass wrote them; `ValueError`/`FileExistsError` become `ChainError`.
ATP TIER01/attempt_002: identical 7/10 (`features.csv`, `labels.csv`, `column_dictionary.json`, `resolved_config.json`, `rejections.csv`, `validation.json`, `stderr.txt`); differing: `summary.json` (2 leaves, both WTA02-revision additions: `diverged_serve_return_history_summaries: {}` and the note sentence describing it), `manifest.json` (`executed_builder` path/hash -> module/hash/package_version, added `declared_binding`, and `outputs["summary.json"]` bytes/sha256 following the summary above), `stdout.txt` (the summary document plus `manifest_sha256` and `output_dir`, which is now the workspace-relative `work/TIER01/run_002/run/features` instead of an absolute path).
WTA WTA02/attempt_002: identical 8/10 (`features.csv`, `labels.csv`, `column_dictionary.json`, `resolved_config.json`, `rejections.csv`, `summary.json`, `validation.json`, `stderr.txt` — the WTA divergence counts reproduce exactly); differing: `manifest.json` (`executed_builder` -> code receipt, added `declared_binding`), `stdout.txt` (`manifest_sha256` and the workspace-relative `output_dir`).
Label file: `labels.csv` is written by `write_labels` and holds exactly `LABEL_HEADER` — `match_id`, `calendar_year`, `source_season`, `match_date`, `tourney_id`, `identity_tier`, `primary_target`, `a_won`, `status`, `source_field_agreement`. `a_won` and `status` appear nowhere in `features.csv`; `column_dictionary.json` publishes the same list as `label_file_columns` and `validate_written_outputs` checks the two files row-for-row by `match_id`.
Label reads: the panel's `a_won` is parsed in `parse_record` and carried only into `label_row`; past results update the Elo state in `EloHistory.apply_batch`; the written label is checked against the parsed record in `validate_written_outputs`. All three are marked `# outcome-history read`. No proper score is computed. The lagged-market Elo uses the normalized Pinnacle probability as a pseudo-outcome and never the winner.
Cutoff: unchanged. `lag_calendar_days = 2` is asserted fixed in `load_config`; every row carries `eligible_through_date = match_date - 2 days`; each state's `*_latest_source_date` is checked against that cutoff both when the row is built and again when the written file is validated. The per-column latest-source-date lineage (`elo_`, `count_`, `workload_`, `lagged_market_latest_source_date`) is kept.
Learned constants: none fit here; the frozen MULTI01 parameter block (half-life 180 d, 50 prior units, K=32, scale 400, windows 7/28 d, rest cap 90 d, staleness 14 d) is asserted equal to the declared values and refuses any drift.
Open: nothing.
