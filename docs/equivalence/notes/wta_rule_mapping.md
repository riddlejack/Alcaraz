# wta_rule_mapping (stage `rule_mapping`, WTA)
Base revision: WTA02_models/wta_rule_rows.py + wta_rules.json. There is no TIER01 counterpart: `carry_rule_rows.py` *extends* an existing mapping and there is no WTA base mapping to extend, so nothing was merged; the ATP tour has `tennislab.panel.rules` (see `rule_mapping.md`).
Module: `tennislab.panel.wta_rules`, with the era table copied to the package resource `src/tennislab/panel/wta_rules.json` (sha256 48194cefcd5657c02c17aa4871c3b4c9e0585bf90d78a900e7d756f4346fffa8, the hash the run's config declares for `references/WTA02_models/wta_rules.json`). A config that declares `rule_config` still reads and hash-verifies the file it names — which is what the WTA02 run does, so the packaged copy is not on the verified path here; it is the default only when the config omits the key, and is then recorded as `{"resource": "tennislab.panel/wta_rules.json", "sha256": ...}`.
ATP TIER01/attempt_002: not this module — see `rule_mapping.md`.
WTA WTA02/attempt_002: identical 6/7 (rules.csv, rule_rows_refused.csv, rule_evidence_by_edition.csv, rule_editions_without_observed_evidence.csv, stdout.txt, stderr.txt); differing: manifest.json (5 leaves — `inputs.code` was the archive file's path and hash and is now `code_receipt("tennislab.panel.wta_rules")`, and `manifest_sha256_of_inputs` is the canonical hash *of that map*, so it moves with it. Every other leaf identical: 46,827 panel rows, 46,827 rule rows, 0 refused, the same 15 rule groups including 4,342 `_carried_forward` rows, and 383 editions with no observed evidence.)
Label reads: none. The stage reads no serve count, no price, no score and no label; the deciding-set inventory it joins was computed once in the event-map folder and is bound by hash.
Learned constants: the era table itself. Each era carries its own basis string naming the inventory observation it rests on (e.g. `inventory_deciding_sets_run_past_6_all_every_season_2007_2018`), and the manifest's `rule_evidence_by_edition.csv` records, per edition, whether the inventory corroborates, is silent on, or contradicts the era applied. `era_carry_from_year = 2024` is the declared WTA02 assumption: a carried row's group ends in `_carried_forward` and its basis names the source season, so it is never mistaken for a declared one.
Open: nothing.
