# wta_sr03_calibration (WTA02/attempt_002, stage sr03_calibration)
Base revision: TIER01_models/sr03_calibrate.py with the WTA02_models/sr03_calibrate.py additions merged (module `tennislab.dynamics.calibrate` over `tennislab.dynamics.calibration`; see sr03_calibration.md for the binding shapes accepted). This run exercises the WTA02 switches: `tour = "WTA"` with `panel_binding_authority = point_run_manifest` (the panel hash is bound to the replay's run manifest; SR02-C1's frozen ATP panel hash is recorded as superseded), outer years 2022–2026 from the `year_plan`, `annual_eligible_floor_year = 2016`, and `calibration.score_years_max = 2024`: `scoring_boundary.json` is written, fits and predictions for 2022–2026 are persisted, and only calibration years 2022–2024 are scored (2025 and 2026 suppressed until the report stage).
WTA WTA02/attempt_002: identical 13/14 (start.json, fit_events.jsonl, fits.json, training_membership.csv, predictions.csv, predictions_boundary.json, scoring_boundary.json, metrics.csv, reliability.csv, comparisons.json, cohort_counts.json, stdout.txt, stderr.txt); differing: run_manifest.json (1 leaf: `code` block added — package receipts plus `declared_binding`) — provenance only.
ATP TIER01/attempt_002: see sr03_calibration.md.
Label reads: as in sr03_calibration.md — `load_dataset` reads `a_won` for every played selected match 2011–2026 (the reserved 2025–2026 outcomes included); the 2025 and 2026 slopes are fitted on 2022–2024/2023–2025 outcomes (history reads for the outer year, but 2025 outcomes enter the 2026 fit); `evaluate` scores only 2022–2024 here because of the ceiling. The integration pass must keep `score_years_max` (or the report barrier) in force for every run whose outer years reach a target year.
Learned constants: per outer year and family one slope (`fits.json`), horizon = the three calendar years before the outer year through 30 December, membership hashes recorded.
Open: nothing.
