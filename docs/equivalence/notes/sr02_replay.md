# sr02_replay
Base revision: TIER01_models/sr02_replay.py (CONFIRM2026 revision 8 + lower-tier feed + same-event-qualifying ablation). WTA02 additions merged under `sr02_replay.*` switches: `relabelled_count_block_statuses` (default `["quarantined_invalid"]`), `count_history_from_year` (default null), and the per-year dynamic dispersion report. The frozen `execution_binding` (archive path + sha256 of `dynamic.py`/`path_runner.py`) is read and recorded as `code.declared_binding`; the package modules `tennislab.dynamics.dynamic` / `.path_runner` run and are recorded by `code_receipt`. Top-level `tour` (absent = ATP) selects the archive revision's *reporting* shape — manifest `id`, the `tier_feed`/`tier_feed_rule_basis` keys (TIER01) versus `dynamic_dispersion_by_year`/`years_with_a_constant_dynamic_probability` (WTA02), the `limits` text and the stdout summary line — so each archive run reproduces exactly; every numerical switch is honoured regardless of tour.
ATP TIER01/attempt_002: identical 6/7 (selected_matches.csv, selection_map.csv, selection_carry_forward.csv, stale_state_rows.csv, stdout.txt, stderr.txt); differing: run_manifest.json (7 leaves, all under `code`: `replay.path/sha256` -> `replay.{module,sha256,package_version}`; `dynamic`/`path_runner` archive sha -> package receipts; `declared_binding` added) — provenance only.
WTA WTA02/attempt_002: see wta_sr02_replay.md.
Label reads: none (`dynamic.SOURCE_ROW_FIELDS` excludes `a_won`; `merge_selected_matches` refuses a label or price column).
Learned constants: none (no reselection; the saved 2011–2024 candidate ids are replayed and a later year inherits the last saved one, recorded per year in selection_carry_forward.csv).
Open: nothing. Note for integration: the dispersion report is computed only under the WTA02 shape; it is cheap and provenance-only and could be always-on once the manifest contract is unified.
