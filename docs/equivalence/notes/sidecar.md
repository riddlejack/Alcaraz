# sidecar
Base revision: WTA02_models/build_sidecar.py (TIER01_models/build_sidecar.py plus one change: the DOB locator string was the literal `data/raw/BIO01/attempt_001/atp_players.csv:<line>` and now names the config's bound `bio_players` path, because BIO01 covers only the ATP; an ATP run binding BIO01's file produces the identical locator, which the run below confirms). Ported changes: the `BIO_PLAYERS_PATH` global and `configure_bio_players_path` become an explicit argument threaded to `age_fields`; `ROOT / binding["path"]` and `(ROOT / declared).resolve()` become `resolve_under_root`; the "write beside the builder" fallback is removed — a run must declare `output.sidecar`; `executed_builder` is `code_receipt(__name__)`; `RuntimeError` becomes `ChainError`. The stage is invoked with the program path as argv[0] (`references/<models>/build_sidecar.py --config ...`); the harness strips the program, so `main(argv)` receives `--config <path>`.
ATP TIER01/attempt_002: identical 8/9 (`trait_latent_sidecar.csv`, `trait_lineage_groups.csv.gz`, `trait_conflicts.csv`, `column_dictionary.json`, `summary.json`, `validation.json`, `stdout.txt`, `stderr.txt`); differing: `manifest.json` (4 leaves, all `executed_builder`: the archive's repository path and file hash become the module name, package file hash and package version).
WTA WTA02/attempt_002: identical 8/9 (same list); differing: `manifest.json` (the same 4 `executed_builder` leaves).
Label reads: none. The stage reads the base feature file, the corrected panel, the player table (for `player_id` and `dob` only) and the SR02 selected-match file; `labels.csv` is deliberately not read and is recorded under `not_read.labels` with the hash the bound base manifest declares. `FORBIDDEN_OUTPUT_FIELDS` is checked against the sidecar schema so no outcome or price column can enter it.
Cutoff: unchanged. `build_sidecar_rows` re-derives `match_date - 2 days` and refuses a base row whose `eligible_through_date` disagrees; trait selection is `bisect_right` on source dates at that cutoff, and every referenced lineage group is re-checked to be no later than it. The per-field latest-source-date lineage (`*_source_group_id`, `*_lag_days`, `*_source_date`, and the `trait_lineage_groups.csv.gz` group rows) is kept.
Learned constants: none.
Open: nothing.
