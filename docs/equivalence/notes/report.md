# report
Base revision: TIER01_models/report.py (tier contrasts, configured primary and secondary contrasts, configured bootstrap seed) with WTA02_models/report.py additions merged under the tour switch (identity and tour from the config, the cohort label, the published bootstrap resampling unit with both units always computed, the dispersion block carried from the predictor config, `tour` in `start.json`). Module: `tennislab.evaluation.report`; the proper-score arithmetic (log loss, Brier, paired contrast deltas, the block bootstrap, reliability bins, the t half-width) is in `tennislab.evaluation.scores` as pure functions over arrays, and `report._read_source_tables` is the package's only read of `labels.csv` for a target year.
ATP TIER01/attempt_002: identical 13/16 — annual_metrics.csv, pooled_metrics.csv, coverage.csv, annual_contrasts.csv, contrast_summary.csv, bootstrap.csv, reliability.csv, selection_diagnostics.csv, selection_receipts.json, primary.json, report.md, stdout.txt, stderr.txt; differing: start.json (`started_utc`; `binding_hashes.reporter` is the package file's hash; `declared_binding` and `code` added), result.json (`completed_utc`), artifact_manifest.json (the hashes and byte counts of those two files). Reproduced headline: `full_tier_minus_full` equal-year log-loss delta −0.006768047240019573 on n 18,972 (match-weighted −0.006777923130547691; t7 95% [−0.008501135362098365, −0.005034959117940781]; tournament-edition bootstrap, seed 20260912, [−0.008721138808835597, −0.004806829579408022]); secondary `base_tier_minus_base` −0.006423689137257632 [−0.008203937566732123, −0.00469097396083813]; pooled match-weighted log loss on the 18,882 priced rows: base 0.6121, full 0.6053, full_tier 0.5984, full_tier_noqual 0.5990, Pinnacle calibrated 0.5873.
WTA WTA02/attempt_002: identical 13/16 (the same thirteen files); differing: the same three provenance files. Reproduced headline: `full_minus_base` equal-year log-loss delta −0.0044273519249091045 on n 4,296 (t1 95% [−0.04198682663992421, 0.033132122790106]; published match-unit bootstrap, seed 20260912, [−0.0075814171837153286, −0.0014054706675700832]; tournament-edition unit [−0.007948109219387945, −0.0008559508581616884]); pooled match-weighted log loss on the 2,344 priced rows: base 0.6230, full 0.6153, Pinnacle calibrated 0.5952.
Label reads: `labels.csv` read for scoring at `report._read_source_tables` (marked `# target-year outcome read`), after every forecast hash, receipt and membership check has passed; nothing before it in the package scores an outcome.
Learned constants: none (the reporter refits nothing; slopes and candidates are copied from the selection ledger into selection_receipts.json).
Open: nothing. Two replacements of resolving behaviour: (1) the archive resolved the selection ledger's recorded absolute predictor-config path; the port binds the predictor config through the reporting config's own `inputs.predictor_config` hash binding, requires the ledger's `config_sha256` to equal that file's hash, and requires `raw_complete.json` and `selection_complete.json` to record the same path string. (2) `_inside_root` is lexical workspace containment (`resolve_under_root`), never `Path.resolve()`. The `code.reporter_sha256` binding is verified only when `code.reporter_path` names `tennislab.evaluation.report`; the archive's file binding is recorded in `start.json` as `declared_binding`. Every recorded digest passes `require_nonempty_digest` (B5); the primary contrast, secondary contrasts, seed and unit come from the reporting config's `settings`, and `tests/test_evaluation_report.py` checks the artifact records the configured pair (B6).
