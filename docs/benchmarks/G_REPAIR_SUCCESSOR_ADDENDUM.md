# Lane G bounded successor repair addendum

## Authority and fixed starting point

This addendum authorizes one bounded successor repair from commit
`de0f6e4e12e5756eefa4783a04e08fba2fb08a2f` (tree
`b69d4d4d0622e5822ba83117500b2c289ebc55b2`) in response to the independent
reconstruction `LANE_G_REPAIR_RECONSTRUCTION.md`, SHA-256
`e60432b8d15d8572d8d9b4a7d8c92d271abed8b455c9eaeb7fa8cb216651df1e`.

The original design, rejected review, frozen repair contract/config, public preflight
failures, passed G1/G2 controls, and passed G7 arithmetic remain preserved. This work does
not authorize a real fit, value join, score, runtime freeze, new model family, broader
audit, integration, merge, push, publication, traffic, or independent-custody claim.

## Exact successor scope

The implementation may change only as needed to:

1. derive `rank_and_calibration_past` from every actual admitted rank-training and
   calibration row, with reconstructable union and fold-specific memberships;
2. require the forecast and barrier attempt to equal the requested attempt, bind the
   original completed producer outputs rather than accepting a transplanted or coherently
   rewritten forecast, verify deterministic saved calibration/incumbent consistency, and
   include `tennislab.dynamics.market` in the effective-code receipt;
3. replace whole-file sensitive-scan exemptions with exact typed receipt schemas and scan
   every unrecognized or non-benign nested field;
4. enforce the fixed synthetic root `fixtures/synthetic`, match-ID prefix `SYNTHETIC-`,
   maximum 1,000 rows per input, and manifest name `fixture_manifest.json` independently
   of caller overrides; and
5. enforce primary block length `8`, sensitivities `[4, 13]`, and the exact supported RNG,
   stream, inference-family, fixed-sensitivity, and deferral descriptors.

Acceptance is limited to reproducing the named plants, making the smallest repair, and
passing the affected controls plus one legitimate synthetic forecast/barrier/report flow.
The full repository suite remains outside this successor pass until independent review.
